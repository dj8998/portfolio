export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1 style={{ marginTop: 0 }}>Loading…</h1>
      <p>
        If you see this page in production, the rewrite to <code>/index.html</code>{" "}
        didn’t apply. Open <code>/index.html</code> directly.
      </p>
    </main>
  );
}

